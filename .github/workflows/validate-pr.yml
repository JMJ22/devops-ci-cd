name: Validate PR to Develop

on:
  pull_request:
    branches: [develop]
    types: [opened, synchronize, reopened]

jobs:
  smoke-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      
      - name: Run Smoke Tests
        run: |
          echo "🧪 Validando: Testes de fumaça no PR..."
          # Seus comandos reais

  unit-tests:
    needs: smoke-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      
      - name: Run Unit Tests
        run: |
          echo "⚡ Validando: Testes unitários no PR..."
          # Seus comandos reais

  build-validation:
    needs: unit-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      
      - name: Build Validation
        run: |
          echo "🔨 Validando: Build do aplicativo..."
          # Build de validação (não deploy)
